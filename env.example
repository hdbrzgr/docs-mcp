# Google Docs MCP Server Environment Configuration

# Authentication Method 1: Service Account (Recommended for server applications)
# Uncomment and set the path to your service account JSON key file
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Authentication Method 2: OAuth2 Client Secrets (For user-based access)
# Uncomment and set the path to your OAuth2 client secrets JSON file
# GOOGLE_CLIENT_SECRETS=/path/to/client-secrets.json

# OAuth2 Token Storage (Optional)
# Path where OAuth2 tokens will be stored (default: token.json)
# GOOGLE_TOKEN_PATH=/path/to/token.json

# Server Configuration
# Uncomment to run in HTTP mode on a specific port
# HTTP_PORT=8080

# Logging Configuration (Optional)
# Set log level: debug, info, warn, error
# LOG_LEVEL=info
